name: 🎷 Daily Encore Report

on:
  schedule:
    # KST 00:00 == UTC 15:00 (previous day)
    - cron: "0 15 * * *"
  workflow_dispatch:
    inputs:
      date:
        description: "Target date (KST) in YYYY-MM-DD"
        required: false

jobs:
  report:
    runs-on: self-hosted
    steps:
      - name: 🎹 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: 🥁 Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest
          run_install: false

      - name: 📦 Install deps
        run: pnpm install --frozen-lockfile

      - name: 🎷 Generate report
        env:
          GITHUB_TOKEN: ${{ secrets.MIKU_GITHUB_TOKEN }}
        run: |
          DATE_INPUT='${{ inputs.date }}'
          if [ -n "$DATE_INPUT" ]; then
            node scripts/generate-daily-report.mjs "$DATE_INPUT"
          else
            node scripts/generate-daily-report.mjs
          fi

      - name: 🎤 Commit & push
        env:
          PUSH_TOKEN: ${{ secrets.MIKU_GITHUB_TOKEN }}
        run: |
          git config user.name "miku-agent"
          git config user.email "miku-agent@users.noreply.github.com"

          # self-hosted runner에서 main에 push하려면 PAT를 사용해 origin을 재설정합니다.
          git remote set-url origin "https://x-access-token:${PUSH_TOKEN}@github.com/miku-agent/01_miku-dashboard.git"

          # git diff는 untracked 파일을 감지 못해서, status로 변경 여부를 확인해요.
          if [ -z "$(git status --porcelain)" ]; then
            echo "No changes to commit."
            exit 0
          fi

          git add reports/daily/*.md

          if [ -z "$(git status --porcelain)" ]; then
            echo "No staged changes to commit."
            exit 0
          fi

          git commit -m "chore(report): daily encore report"
          git push origin HEAD:main
